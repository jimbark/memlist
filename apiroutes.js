// Native Express API

// NOTE: better to use REST API module, so this is only here for reference

// import handlers from api.js module
var api = require('./handlers/api.js');

module.exports = function(app){

    // example route; get a list of Attractions
    //app.get('/api/attractions', api.getAttractionList);


};
